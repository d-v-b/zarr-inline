{
  "name": "zarr-json",
  "version": "0.1.0",
  "private": true,
	"engines": {
		"node": ">=21"
	},
  "description": "Store a Zarr v3 hierarchy as a single JSON object (TypeScript reference implementation)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test \"dist/tests/*.test.js\""
  },
  "dependencies": {
    "zarrita": "latest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
